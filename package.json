{
  "name": "tulx",
  "version": "0.1.0",
  "private": true,
  "description": "Utility library with documentation website",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "test": "turbo run test",
    "test:run": "turbo run test:run",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\"",
    "type-check": "turbo run type-check",
    "check": "pnpm format:check && pnpm type-check && pnpm lint",
    "check:all": "echo '🔍 Checking format...' && pnpm format:check && echo '✅ Format check passed' && echo '🔍 Type checking...' && pnpm type-check && echo '✅ Type check passed' && echo '🔍 Linting...' && pnpm lint && echo '✅ Lint passed' && echo '🔍 Running tests...' && pnpm test:run && echo '✅ All checks passed'",
    "fix": "pnpm lint:fix && pnpm format && pnpm lint:fix",
    "fix:all": "echo '🔧 Step 1/4: Running lint fixes...' && pnpm lint:fix && echo '✅ Lint fixes applied' && echo '🔧 Step 2/4: Running format fixes...' && pnpm format && echo '✅ Format fixes applied' && echo '🔧 Step 3/4: Running lint fixes again...' && pnpm lint:fix && echo '✅ Second lint fixes applied' && echo '🔍 Step 4/4: Type checking...' && pnpm type-check && echo '✅ Type check passed' && echo '✅ Fix process completed successfully'",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:status": "changeset status",
    "version": "pnpm changeset:version && pnpm install",
    "release": "pnpm build && pnpm test:run",
    "publish:utils": "cd packages/utils && npm publish --access public && cd ../..",
    "publish:check": "pnpm changeset:status",
    "publish:prepare": "pnpm version && pnpm release",
    "publish:full": "pnpm publish:prepare && pnpm publish:utils"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "prettier": "^3.2.5",
    "turbo": "^2.1.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
