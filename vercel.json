{
  "buildCommand": "pnpm install && turbo run build --filter=@tulx/docs",
  "outputDirectory": "apps/docs/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "rootDirectory": "apps/docs"
}

